simple-sample-pipeline:
  workers: 1
  delay: "1000"
  source:
    # http:
    #   ssl: false
    #   authentication:
    #     unauthenticated:
    #   port: 2021
    # random:
    file:
      path: "test.log"
      format: "json"
      record_type: "event"
  processor:
    - string_converter:
        query:
          - "source = stdin | stats count()"
          - "test2"
  sink:
    - stdout:
    # - opensearch:
    #     hosts: [ "http://localhost:9200" ]
    #     insecure: true
    #     # username: admin
    #     # password: admin
    #     index: data_prepper_results

