simple-sample-pipeline:
  workers: 1
  delay: "15000"
  source:
    # http:
    #   ssl: false
    #   authentication:
    #     unauthenticated:
    #   port: 2021
    random:
    # file:
    #   path: "/home/lijshu/projects/os-2.0/opensearch-dashboards/dashboards.log"
    #   format: "json"
    #   record_type: "event"
  processor:
    - string_converter:
        upper_case: "source = stdin | stats count()"
  sink:
    - stdout:
    # - opensearch:
    #     hosts: [ "http://localhost:9200" ]
    #     insecure: true
    #     # username: admin
    #     # password: admin
    #     index: data_prepper_results

